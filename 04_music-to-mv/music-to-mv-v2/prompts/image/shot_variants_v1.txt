Generate materially distinct image prompt variants for repeated chorus shots.
Return JSON only. The first character must be `[`. Do not output analysis, explanations, markdown, XML tags, or <think> content.

## Global Visual Bible (must not break)
{{ visual_bible }}

{% if char_prompt %}## Character Continuity Reference
Only use when a person appears in the shot.
{{ char_prompt }}
{% endif %}

## Core Creative Constants
- Global Fixed Style: {{ style }}
- Core Mood Tone: {{ mood }}
- Music: {{ music_style }}

## Variant Production Tasks
{{ variant_tasks }}

## Allowed Variation Axes
Choose one clear change per variant: shot_distance, foreground, lighting, symbolic_focus, subject_presence, or motion_rhythm.

## Disallowed Axes (never change)
- world setting or era
- core palette family
- protagonist identity
- song meaning or mood

## Requirements
- Each variant 16-28 English words.
- Each variant must feel like a different usable shot, not a duplicate with swapped adjectives.
- Keep the same lyrical core, world, and style.
- Output pure JSON array only. Start with `[` and end with `]`.
- Do not include markdown code fences.

[
  {"scene_id": 1, "variant_id": 1, "prompt": "..."}
]
