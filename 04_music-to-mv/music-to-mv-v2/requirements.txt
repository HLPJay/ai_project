# ═══════════════════════════════════════════════════════
# Music-to-MV v2 — 运行依赖
# ═══════════════════════════════════════════════════════

# ── 必需依赖（运行 `python -m src.main` 必须）───────
PyYAML>=6.0              # 读取 prompts/registry.yaml 配置

# ── 可选依赖 ──────────────────────────────────────────
# Jinja2 增强的模板渲染（PromptRegistry 内置引擎也可工作）
jinja2>=3.0

# 数值计算（torch/numpy 依赖）
numpy>=1.24

# 歌词对齐（不装则回退到基础均匀时间戳 SRT，或使用 manual SRT）
faster-whisper            # ASR 转写，优先用于歌词时间轴对齐
openai-whisper            # fallback ASR 后端

# 人声分离（提升对齐精度）
demucs                    # Meta 出品，分离人声和伴奏
soundfile                 # 音频文件读写
torchcodec                # torch 视频编解码支持

# PyTorch（图片生成、深度学习模型支持）
torch
torchaudio
torchvision

# 图片处理（dry_run 模式生成文本占位图）
Pillow>=10.0

# ── 测试依赖 ──────────────────────────────────────────
pytest>=7.0
pytest-html>=3.0

# ── 安装方式 ──────────────────────────────────────────
# pip install -r requirements.txt          # 安装所有（含测试）
# pip install pyyaml                       # 仅必需
